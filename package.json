{
  "name": "sync-gui",
  "version": "1.1.1",
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/hamidrg20001379/sync-gui-public.git"
  },
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "node scripts/prepare-win-tools.mjs && tauri build",
    "build:linux": "tauri build",
    "build:win": "node scripts/prepare-win-tools.mjs && tauri build --target x86_64-pc-windows-msvc",
    "prepare:win": "node scripts/prepare-win-tools.mjs",
    "next:dev": "next dev --port 49173",
    "next:build": "next build",
    "test": "node --test test/"
  },
  "dependencies": {
    "@phosphor-icons/react": "^2.1.10",
    "@tauri-apps/api": "^2.11.1",
    "next": "latest",
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  }
}
